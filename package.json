{
  "name": "node-parse-thingtrack",
  "version": "0.0.2-6",
  "description": "parse file bynary tachograf in json",
  "keywords": "tgd,parse,file tacograf",
  "repository": {
    "type": "git",
    "url": "https://github.com/negrero/node-parse-thingtrack",
    "scripts": {
      "publish-prerelease": "npm version prerelease && npm run push",
      "publish-prepatch": "npm version prepatch && npm run push",
      "publish-patch": "npm version patch && npm run push",
      "publish-minor": "npm version minor && npm run push",
      "publish-major": "npm version major && npm run push",
      "push": "git push origin master --follow-tags && npm publish",
      "prepublish": "echo \"do whatever\""
    }
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "andres carmona gil",
  "license": "ISC",
  "dependencies": {
    "java": "^0.6.1"
  }
}
